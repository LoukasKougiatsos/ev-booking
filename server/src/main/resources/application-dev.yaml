spring:
  datasource:
    url: jdbc:postgresql://localhost:55432/ev_booking
    username: ev_user
    password: dev_password
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

app:
  ev:
    api-key: ${EV_API_KEY:}
